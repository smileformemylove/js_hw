'use strict';

const userNumber = Number(prompt("Введіть перше число: "));
const userNumber2 = Number(prompt("Введіть друге число: "));
const userNumber3 = Number(prompt("Введіть третє число: "));

const arithmetic = ((userNumber + userNumber2 + userNumber3) / 3);

alert(`Середнє арифметичне ваших числ буде: ${arithmetic}`);

